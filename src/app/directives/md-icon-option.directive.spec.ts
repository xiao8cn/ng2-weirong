import { MdIconOptionDirective } from './md-icon-option.directive';

describe('MdIconOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new MdIconOptionDirective();
    expect(directive).toBeTruthy();
  });
});
